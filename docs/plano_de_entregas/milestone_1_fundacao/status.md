# Status - Milestone 1: Fundação do App e Lazy Login

## To Do (A Fazer)
- (Vazio)

## In Progress (Em Andamento)
- (Vazio)

## Done (Feito)
- [x] Instalar e configurar módulo PWA (ex: `next-pwa` ou Serwist) e criar `manifest.json`.
- [x] Criar estrutura de banco de dados e tabela `users` (migrations locais).
- [x] Instalar o SDK `@supabase/supabase-js` e criar `lib/supabaseClient.ts`.
- [x] Criar a lógica cliente no App Router para geração e salvamento do Device ID Fantasma (Lazy Login).
- [x] Iniciar os containers Docker do Supabase (`npx supabase start`).
- [x] Inicializar o projeto Supabase localmente (`npx supabase init`).
- [x] Rodar o comando de criação do Next.js App no diretório.
- [x] Limpar o boilerplate padrão (arquivos não usados).
- [x] Configurar a paleta de cores (Dark mode) no `tailwind.config` / `globals.css`.
